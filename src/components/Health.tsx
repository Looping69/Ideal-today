export default function Health() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-50">
            <pre className="text-lg font-mono text-green-600">OK</pre>
        </div>
    );
}
